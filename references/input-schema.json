{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Jewelry Recognition Input",
  "description": "Minimal input for brand-site jewelry recognition. Agent locates the item page and extracts data.",
  "type": "object",
  "required": ["brand", "vendor_item_number", "source_url"],
  "properties": {
    "brand": {
      "type": "string",
      "description": "Brand name."
    },
    "vendor_item_number": {
      "type": "string",
      "description": "Vendor item number, SKU, or model reference. Agent searches for this on the brand site."
    },
    "source_url": {
      "type": "string",
      "format": "uri",
      "description": "Brand site URL to start search from. Can be homepage or any page on the brand domain."
    }
  }
}
